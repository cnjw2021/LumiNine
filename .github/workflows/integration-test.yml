name: Integration Tests (Manual)

on:
  workflow_dispatch:
    inputs:
      test_path:
        description: 'Specific test path to run (leave empty for all integration tests)'
        required: false
        default: ''

env:
  DB_NAME: luminine
  DB_USER: superuser
  DB_PASSWORD: superpass!

jobs:
  integration-test:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare environment files
        run: |
          # Root .env for docker-compose variable interpolation
          echo "DB_NAME=$DB_NAME" > .env
          echo "DB_USER=$DB_USER" >> .env
          echo "DB_PASSWORD=$DB_PASSWORD" >> .env
          echo "DB_HOST=postgres" >> .env
          echo "DB_PORT=5432" >> .env

          # Backend .env for Python app
          echo "SECRET_KEY=testsecretkey" > backend/.env.development.backend
          echo "JWT_SECRET_KEY=testjwtsecretkey" >> backend/.env.development.backend
          echo "DB_USER=$DB_USER" >> backend/.env.development.backend
          echo "DB_PASSWORD=$DB_PASSWORD" >> backend/.env.development.backend
          echo "DB_HOST=postgres" >> backend/.env.development.backend
          echo "DB_NAME=$DB_NAME" >> backend/.env.development.backend
          echo "DB_PORT=5432" >> backend/.env.development.backend
          echo "DATABASE_URL=postgresql+psycopg2://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}" >> backend/.env.development.backend
          echo "SUPERUSER_EMAIL=superuser" >> backend/.env.development.backend
          echo "SUPERUSER_PASSWORD=$DB_PASSWORD" >> backend/.env.development.backend
          echo "FLASK_DEBUG=1" >> backend/.env.development.backend

          # Frontend .env for Next.js (minimal, for Docker build)
          echo "NEXT_PUBLIC_API_URL=http://localhost:5001" > frontend/.env.development.frontend
          echo "NODE_ENV=development" >> frontend/.env.development.frontend

          mkdir -p backend/logs backend/.pytest_cache
          chmod -R 777 backend/logs backend/.pytest_cache

      - name: Seed Database (SQL + CSV)
        run: |
          echo "### Seeding database with full data (SQL + CSV)... ###"
          make db-seed

      - name: Run Integration Tests
        run: |
          if [ -n "${{ github.event.inputs.test_path }}" ]; then
            echo "### Running specific test: ${{ github.event.inputs.test_path }} ###"
            docker compose run --rm backend-test pytest ${{ github.event.inputs.test_path }} -v
          else
            echo "### Running all integration tests ###"
            make test-integration
          fi

      - name: Dump Docker Logs on Failure
        if: failure()
        run: |
          docker compose logs backend postgres
